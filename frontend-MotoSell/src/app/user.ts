export interface User {
    id: string
    username: string
    password: string
    email: string
    first_name: string
    last_name: string

    // auth_token: string
    // refresh_token: string
}
